name = "COBOL"
grammar = "cobol"
path_suffixes = ["cbl", "CBL", "cob", "COB", "cpy", "CPY", "cobol"]
line_comments = ["*> "]
autoclose_before = ".,)"
brackets = [
    { start = "(", end = ")", close = true, newline = false },
    { start = "\"", end = "\"", close = true, newline = false, not_in = ["string"] },
    { start = "'", end = "'", close = true, newline = false, not_in = ["string", "comment"] },
]
