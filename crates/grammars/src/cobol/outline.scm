(identification_division
  (program_name) @name) @item

(section_header
  name: (_) @name) @item

(paragraph_header
  name: (_) @name) @item

; The level number is context rather than part of the name, so that searching
; for a data item finds it by the name the code writes.
(data_description
  (level_number) @context
  (entry_name) @name) @item

(constant_entry
  name: (_) @name) @item
